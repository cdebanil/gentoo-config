# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=native -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

MAKEOPTS="-j8"
EMERGE_DEFAULT_OPTS="--jobs=8 --load-average=8"
ACCEPT_LICENSE="@BINARY-REDISTRIBUTABLE"
ACCEPT_KEYWORDS="amd64"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

USE="bluetooth networkmanager alsa pgo man unicode dbus udisks elogind dist-kernel opengl vulkan jpeg png gif tiff webp -kwallet -acl -telemetry -neon -pcmcia -coreaudio -css -ieee1394 -ios -ipod -netifrc -nls -wayland -systemd -kde -gnome -xinerama -Xaw3d -a52 -altivec -aqua -accessibility -cdda -cdr -cups -dvd -dvdr -fortran -X -pulseaudio"

GRUB_PLATFORMS="efi-64"

INPUT_DEVICES="libinput"
VIDEO_CARDS="nvidia intel iris i965"
